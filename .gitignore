# Secrets — never commit (see S-6)
.env
*.env
!.env.example
secrets/
*.key
*.pem
rcon.password
cloudflare-token*
cf-ddns.env

# World data and server runtime — lives on the server, not in Git
world/
world_nether/
world_the_end/
backups/
*.jar
logs/
crash-reports/
cache/

# macOS
.DS_Store
stacks/*/data/
